<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shop.mtcoding.blog.model.UserRepository">
   <select id="findAll" resultType="shop.mtcoding.blog.model.User">
      select * from user_tb
   </select>

   <select id="findById" resultType="shop.mtcoding.blog.model.User">
      select * from user_tb where id = #{id}
   </select>

   <insert id="insert">
      insert into user_tb ( username, password, email, created_at ) 
        values ( #{username}, #{password}, #{email}, now() ) 
   </insert>

   <update id="update">
      update user_tb set password = #{password}, email=#{email}, created_at=now() where id = #{id}@
   </update>

   <delete id="delete">
      delete from user_tb where id = #{id}
   </delete>
</mapper>